
// using System.Diagnostics;
// using System.Reflection;

static public class Utilities
{
    // static public bool checkCallerClassUsingStackTrace(string className)
    // {
    //     //we skip the frame 0 so we start tracing from the caller of this method
    //     StackTrace stackTrace = new StackTrace(skipFrames: 1, fNeedFileInfo: false);

    //     StackFrame callerFrame = stackTrace.GetFrame(0);
    //     MethodBase callermethod = callerFrame.GetMethod();

    //     string callerClassName = callermethod.DeclaringType.Name;

    //     return callerClassName == className;
    // }
}
